# This file has been generated by `make_travis_yml_2.hs`
# see https://github.com/hvr/multi-ghc-travis for more information
language: c
sudo: false

git:
  submodules: false  # whether to recursively clone submodules

cache:
  directories:
    - $HOME/.cabal/packages
    - $HOME/.cabal/store


# We whitelist branches, as we don't really need to build dev-branches.
# NB: remember to add release branches.
branches:
  only:
    - master

before_cache:
  - rm -fv $HOME/.cabal/packages/hackage.haskell.org/build-reports.log
  # remove files that are regenerated by 'cabal update'
  - rm -fv $HOME/.cabal/packages/hackage.haskell.org/00-index.*
  - rm -fv $HOME/.cabal/packages/hackage.haskell.org/*.json
  - rm -fv $HOME/.cabal/packages/hackage.haskell.org/01-index.cache
  - rm -fv $HOME/.cabal/packages/hackage.haskell.org/01-index.tar
  - rm -fv $HOME/.cabal/packages/hackage.haskell.org/01-index.tar.idx

matrix:
  include:
    - env: CABALVER=head GHCVER=7.8.4 ELASTICSEARCH=1.5.2
      compiler: ": #GHC 7.8.4"
      addons: {apt: {packages: [openjdk-jre,cabal-install-head,ghc-7.8.4], sources: [hvr-ghc]}}
    - env: CABALVER=head GHCVER=7.8.4 ELASTICSEARCH=2.3.5
      compiler: ": #GHC 7.8.4"
      addons: {apt: {packages: [openjdk-jre,cabal-install-head,ghc-7.8.4], sources: [hvr-ghc]}}
    - env: CABALVER=head GHCVER=7.10.3 ELASTICSEARCH=1.5.2
      compiler: ": #GHC 7.10.3"
      addons: {apt: {packages: [openjdk-jre,cabal-install-head,ghc-7.10.3], sources: [hvr-ghc]}}
    - env: CABALVER=head GHCVER=7.10.3 ELASTICSEARCH=2.3.5
      compiler: ": #GHC 7.10.3"
      addons: {apt: {packages: [openjdk-jre,cabal-install-head,ghc-7.10.3], sources: [hvr-ghc]}}
    - env: CABALVER=head GHCVER=8.0.1 ELASTICSEARCH=1.5.2
      compiler: ": #GHC 8.0.1"
      addons: {apt: {packages: [openjdk-jre,cabal-install-head,ghc-8.0.1], sources: [hvr-ghc]}}
    - env: CABALVER=head GHCVER=8.0.1 ELASTICSEARCH=2.3.5
      compiler: ": #GHC 8.0.1"
      addons: {apt: {packages: [openjdk-jre,cabal-install-head,ghc-8.0.1], sources: [hvr-ghc]}}

before_install:
 - unset CC
 - export PATH=/opt/ghc/$GHCVER/bin:/opt/cabal/$CABALVER/bin:$PATH

install:
 - travis_retry wget --no-check-certificate https://download.elastic.co/elasticsearch/elasticsearch/elasticsearch-$ELASTICSEARCH.tar.gz
 - tar xzf elasticsearch-$ELASTICSEARCH.tar.gz
 - echo "path.repo = [\"/tmp\"]" >> ./elasticsearch-$ELASTICSEARCH/elasticsearch.yml
 - if [ "x$ELASTICSEARCH" = "x1.5.2" ]; then ES_VERSION_ARG=-v; else ES_VERSION_ARG=--version; fi
 - ./elasticsearch-$ELASTICSEARCH/bin/elasticsearch $ES_VERSION_ARG
 - ./elasticsearch-$ELASTICSEARCH/bin/elasticsearch &
 - cabal --version
 - BENCH=${BENCH---enable-benchmarks}
 - TEST=${TEST---enable-tests}
 - echo "$(ghc --version) [$(ghc --print-project-git-commit-id 2> /dev/null || echo '?')]"
 - travis_retry cabal update -v
 - sed -i 's/^jobs:/-- jobs:/' ${HOME}/.cabal/config
 - cabal new-build ${TEST} ${BENCH} --dep -j2

before_script:
  - sleep 10 # Elasticsearch can be a bit slow to start.

# Here starts the actual work to be performed for the package under test;
# any command which exits with a non-zero exit code causes the build to fail.
script:
 - travis_retry curl -X GET http://localhost:9200
 - if [ -f configure.ac ]; then autoreconf -i; fi
 # this builds all libraries and executables (including tests/benchmarks)
 - cabal new-build ${TEST} ${BENCH}

 # there's no 'cabal new-test' yet, so let's emulate for now
 - SRC_BASENAME=$(cabal info . | awk '{print $2;exit}')
 - TESTS=( $(awk 'tolower($0) ~ /^test-suite / { print $2 }' *.cabal) );
   shopt -s globstar;
   RC=true; for T in ${TESTS[@]}; do echo "== $T ==";
   if dist-newstyle/build/**/$SRC_BASENAME/**/build/$T/$T; then echo "= $T OK =";
   else echo "= $T FAILED ="; RC=false; fi; done; $RC
 - cabal sdist # test that a source-distribution can be generated

 # Check that the resulting source distribution can be built w/o and w tests
 - tar -C dist/ -xf dist/$SRC_BASENAME.tar.gz
 - "echo 'packages: .' > dist/$SRC_BASENAME/cabal.project"
 - cd dist/$SRC_BASENAME/
 - cabal new-build --disable-tests --disable-benchmarks
 - rm -rf ./dist-newstyle
 - cabal new-build ${TEST} ${BENCH}

# EOF
